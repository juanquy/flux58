# Database Configuration
# Set DB_TYPE to 'sqlite' or 'postgres'
DB_TYPE=sqlite

# PostgreSQL connection parameters (only used if DB_TYPE=postgres)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=flux58
DB_USER=postgres
DB_PASS=postgres

# Flask Configuration
# FLASK_ENV=development
# FLASK_DEBUG=1
# FLASK_APP=app.py

# Application Settings
# LOG_LEVEL=INFO
# EXPORT_CONCURRENT_JOBS=2